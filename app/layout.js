import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Inside app/layout.js

export const metadata = {
  title: "Krishna Sai // Creative Frontend Architect",
  description:
    "Personal portfolio displaying interactive, high-fidelity user interfaces and open-source JavaScript applications.",
  icons: {
    icon: "/favicon.ico", // Points to public/favicon.ico or app/favicon.ico
  },
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
